# ══════════════════════════════════════════════════════════════
# WINDI Pioneer Program — nginx configuration snippet
# Add to /etc/nginx/sites-enabled/windi-domain.com
# ══════════════════════════════════════════════════════════════

# 1. Add this upstream (after windi_dragon line ~52):
#
#    upstream windi_pioneer { server 127.0.0.1:8120 max_fails=3 fail_timeout=30s; keepalive 16; }

# 2. Add this location block (inside the main server block, after /dashboard/):
#
#    # ── Pioneer Program Landing (:8120) ────────────────────
#    location /pioneer/ {
#        proxy_pass http://windi_pioneer/pioneer/;
#        proxy_http_version 1.1;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#        proxy_read_timeout 30s;
#        proxy_connect_timeout 10s;
#    }
#    location /api/pioneer/ {
#        proxy_pass http://windi_pioneer/api/pioneer/;
#        proxy_http_version 1.1;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }
#    # ── END Pioneer Program ────────────────────────────────
