# AUDITORIA-RAIZ-INVENTORY-CLOSURE-20260620

Status: CLOSURE SIDECAR / not a seal / no Ledger receipt generated by this file
Target: /opt/windi/docs/audit/WINDI-HIOS-INVENTORY.md
Measured on STRATO: 2026-06-20T14:44:01+02:00
Executor: Codex

## Why this sidecar exists

The inventory file must not contain its own final SHA256 as an inline closing hash after the fact.
Writing a hash into the target file changes the target file and invalidates the hash that was just written.

Therefore, the closure hash is externalized in this sidecar.

## Target Measurement

Target SHA256:

```text
4c219002e60a5ea587a810d1058c6067dabf1eb970b6d1a9f7dce5441072215b
```

Target line count:

```text
490
```

Target byte size:

```text
42294 bytes
```

Target mtime:

```text
2026-06-20 14:40:16.197717591 +0200
```

## Closure Rule

This sidecar closes the measured state above.

If `/opt/windi/docs/audit/WINDI-HIOS-INVENTORY.md` is edited after this measurement, this closure is no longer the current closure. A new `sha256sum` must be measured and a new closure sidecar must be created.

## I11 Note

The previous pattern of declaring a final hash inside a file and then continuing to edit the same file is forensically unsafe. It is not malicious; it is a sequencing error.

Correct order:

```text
finish target
measure target
write external closure
do not touch target under that closure
```

The seal must be the last act over the target state, or the seal describes a state that no longer exists.
